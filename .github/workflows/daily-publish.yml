name: Daily Auto-Publish (21:30 JST)

on:
  schedule:
    - cron: '30 12 * * *'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: 'npm' }
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: npm ci

      - name: Run auto-publish script
        env:
          TZ: Asia/Tokyo
        run: python scripts/auto-publish-2130.py

      - name: Build Astro site
        run: npm run build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=toushi-shippai-roku --branch=main

      - name: Commit logs
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add logs/ approved/ || true
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 auto-publish [skip ci]"
          git push || echo "Nothing to push"

